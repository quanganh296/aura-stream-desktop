{
  "name": "aurastream-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd server && npm install --omit=dev && cd ../client && npm install --include=dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
